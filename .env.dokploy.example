POSTGRES_USER=casemgmt
POSTGRES_PASSWORD=replace-with-strong-db-password
POSTGRES_DB=casemanagement

# Long random secret (at least 64 chars recommended).
JWT_SIGNING_KEY=replace-with-long-random-signing-key

# Public web origin used for API CORS allowlist.
PUBLIC_APP_ORIGIN=https://cases.example.com

# Host port for the web service when running outside Dokploy.
WEB_PORT=8080
