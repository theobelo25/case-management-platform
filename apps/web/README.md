# Web (Angular)

See the [repository README](../../README.md) for stack overview and how to run the app with the API.

**Dev server** (from repo root: `pnpm web:start`, or from this folder):

```bash
pnpm exec ng serve
```

App: `http://localhost:4200/` — API calls are proxied to the backend per `proxy.conf.json`.
